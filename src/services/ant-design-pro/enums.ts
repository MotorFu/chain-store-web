export namespace API_ENUM {
  export enum AccountTypeEnum {
    SuperAdmin = 1,
    StoreAdmin = 2,
    Cashier = 3,
  }

  export enum AccountStatusEnum {
    Normal = 1, // 正常
    Freeze = 2, // 冻结
  }
}
